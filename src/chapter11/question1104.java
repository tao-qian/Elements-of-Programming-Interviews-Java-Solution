package chapter11;

public class question1104 {
	// Here we did not implement the first solution mentioned in the book,
	// because it is too complicated :(.
	// One way to solve this problem is to sort the array first and then use
	// the two pointers approach. It has space O(n) and time O(n), but is
	// significantly
	// easier to implement.
}
